# 🔧 Résoudre l'erreur "Not Found" sur IONOS

## Problème
L'erreur "Not Found" apparaît car React Router utilise le client-side routing. Quand tu accèdes à une route comme `/Generator` ou `/Dashboard`, le serveur web cherche un fichier physique qui n'existe pas.

## Solution

### 1. Vérifier que `.htaccess` est uploadé
Le fichier `.htaccess` doit être dans le dossier `dist/` et uploadé sur IONOS.

### 2. Structure correcte sur IONOS
Dans FileZilla, côté serveur (`/public` ou `/htdocs`), tu dois avoir :

```
/public (ou /htdocs)
  ├── .htaccess          ← IMPORTANT !
  ├── index.html
  └── assets/
      ├── index-XXXXX.css
      └── index-XXXXX.js
```

### 3. Si `.htaccess` n'est pas dans dist/
Copie-le manuellement :
```bash
# Windows PowerShell
Copy-Item public\.htaccess dist\.htaccess
```

### 4. Rebuild et redéploie
```bash
npm run build
npm run deploy:auto
```

### 5. Vérifier sur IONOS
1. Connecte-toi avec FileZilla
2. Va dans `/public` ou `/htdocs`
3. Vérifie que `.htaccess` est bien présent
4. Si absent, upload-le manuellement

### 6. Tester
- Accède à ton domaine : `https://tondomaine.com`
- Accède à une route : `https://tondomaine.com/Generator`
- Les deux doivent fonctionner !

## Alternative : Si IONOS ne supporte pas .htaccess

Si IONOS utilise Nginx au lieu d'Apache, contacte le support pour configurer le routing, ou utilise un fichier `_redirects` (pour Netlify/Vercel).

